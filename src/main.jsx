import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // (Jika ada file CSS tambahan)
import App from './App'; // Impor komponen App

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
